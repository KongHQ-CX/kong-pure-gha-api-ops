_format_version: "3.0"

plugins:
- name: rate-limiting
  operationId: "echoPost"
  config:
    minute: 60
- name: pre-function
  operationId: "echoGet"
  config:
    access:
    - |
      kong.log.warn("per-route pre-function")
